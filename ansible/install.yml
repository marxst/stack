---
- name: base setup
  hosts: stack-nodes
  become: yes
  tags:
    - common

  roles:
    - role: base
      tags: ['base']
    - role: repositories
      tags: ['repositories']
    - role: firewall
      tags: ['firewall']


- name: set up controller node
  hosts: controller-nodes
  gather_facts: no
  become: yes
  tags:
    - controller

  roles:
    - role: memcached
      tags: ['memcached']
    - role: mariadb
      tags: ['mariadb']
    - role: rabbitmq
      tags: ['rabbitmq']
    - role: mongodb
      tags: ['mongodb']
    - role: os_common
      tags: ['os_common']
    - role: keystone
      tags: ['keystone']
    - role: cinder-controller
      tags: ['cinder','cinder-controller']
    - role: reboot
      tags: ['reboot']

- name: set up storage nodes
  hosts: storage-nodes
  gather_facts: no
  become: yes
  tags:
    - storage

  pre_tasks:
    - name: create software raid
      command: mdadm --create {{ raid1_dest_device }} --level=1 --raid-devices={{ raid1_src_devices|length }} {{ raid1_src_devices|join(' ') }}
      args:
        creates: '{{ raid1_dest_device }}'

  roles:
    - role: cinder-storage
      tags: ['cinder','cinder-storage']
    - role: cinder-lvm
      tags: ['cinder','cinder-lvm']

# vim: ft=ansible
